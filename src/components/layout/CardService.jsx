import { Fragment } from "react"
import { Button } from "@components/ui/button"

const CardService = function({ children }){
   return(
      <Fragment>
         <Button>
            This a button
         </Button>
      </Fragment>
   )
}